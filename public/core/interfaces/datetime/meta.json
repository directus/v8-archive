{
  "name": "datetime",
  "version": "1.0.0",
  "dataTypes": {
    "DATETIME": null,
    "TIMESTAMP": null
  },
  "options": {
    "min": {
      "interface": "datetime",
      "comment": "You can use the min and max attributes to restrict the dates that can be chosen by the user"
    },
    "max": {
      "interface": "datetime",
      "comment": "You can use the min and max attributes to restrict the dates that can be chosen by the user"
    },
    "localized": {
      "interface": "toggle",
      "comment": "Show the date localized on the listing views",
      "defaultValue": true
    },
    "showRelative": {
      "interface": "toggle",
      "comment": "Show dates in relatively to now (eg: 3 days ago)",
      "defaultValue": false
    }
  }
}
