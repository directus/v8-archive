{
  "name": "dropdown-multiselect",
  "version": "1.0.0",
  "dataTypes": {
    "VARCHAR": 100,
    "CHAR": 50
  },
  "options": {
    "choices": {
      "interface": "json",
      "comment": "dropdown-multiselect-choices",
      "defaultValue": {
        "option1": "Option 1",
        "option2": "Option 2"
      }
    },
    "placeholder": {
      "interface": "text-input",
      "comment": "Enter placeholder text",
      "defaultValue": "",
      "length": 200
    },
    "wrapWithDelimiter": {
      "interface": "toggle",
      "comment": "Wrap the values with a pair of delimiters to allow strict searching for a single value",
      "defaultValue": true
    },
    "formatting": {
      "interface": "radio-buttons",
      "comment": "The output format on the list view",
      "defaultValue": "text",
      "options": {
        "choices": {
          "text": "Display Text",
          "value": "Value"
        }
      }
    }
  },
  "translations": {
    "en-US": {
      "dropdown-multiselect-choices": "Enter JSON key value pairs with the saved value and text displayed."
    }
  }
}
