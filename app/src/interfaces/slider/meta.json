{
  "name": "$t:slider",
  "version": "1.0.1",
  "types": ["integer"],
  "icon": "tune",
  "recommended": {
    "length": 10
  },
  "options": {
    "minimum": {
      "name": "$t:min",
      "comment": "$t:min_comment",
      "interface": "numeric",
      "default": 0
    },
    "maximum": {
      "name": "$t:max",
      "interface": "numeric",
      "comment": "$t:max_comment",
      "default": 100
    },
    "step": {
      "name": "$t:step",
      "comment": "$t:step_comment",
      "interface": "numeric",
      "default": 1
    },
    "unit": {
      "name": "$t:unit",
      "comment": "$t:unit_comment",
      "interface": "text-input"
    }
  }
}
