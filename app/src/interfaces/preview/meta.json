{
  "name": "$t:preview",
  "version": "1.0.0",
  "types": ["alias"],
  "icon": "visibility",
  "options": {
    "url_template": {
      "name": "$t:url_template",
      "comment": "Test",
      "interface": "text-input",
      "required": true,
      "default": "https://example.com/collection/{{ id }}",
      "options": {
        "placeholder": "https://example.com/articles/{{ id }}/{{ slug }}"
      }
    }
  }
}
