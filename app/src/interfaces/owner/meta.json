{
  "name": "$t:owner",
  "version": "1.0.0",
  "types": ["owner"],
  "icon": "account_circle",
  "relation": "user",
  "recommended": {
    "length": 10
  },
  "options": {
    "template": {
      "name": "$t:template",
      "comment": "$t:template_comment",
      "interface": "text-input",
      "default": "{{first_name}} {{last_name}}"
    },
    "display": {
      "name": "$t:display",
      "comment": "$t:display_comment",
      "interface": "dropdown",
      "default": "both",
      "options": {
        "choices": {
          "both": "$t:avatar + $t:name",
          "avatar": "$t:avatar",
          "name": "$t:name"
        }
      }
    }
  }
}
