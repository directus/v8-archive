{
  "name": "$t:file_size",
  "version": "1.0.1",
  "types": ["integer"],
  "icon": "storage",
  "recommended": {
    "length": 10
  },
  "options": {
    "placeholder": {
      "name": "$t:placeholder",
      "interface": "text-input",
      "comment": "$t:placeholder_comment",
      "length": 200
    },
    "format": {
      "name": "$t:format",
      "comment": "$t:format_comment",
      "interface": "switch",
      "default": true
    },
    "formatInput": {
      "name": "$t:format_input",
      "comment": "$t:format_input_comment",
      "interface": "switch",
      "default": true
    },
    "decimal": {
      "name": "$t:decimal",
      "comment": "$t:decimal_comment",
      "interface": "switch",
      "default": true
    }
  }
}
